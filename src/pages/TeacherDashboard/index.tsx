/**
 * Placeholder page — TeacherDashboard feature is built in its own later phase.
 * Kept intentionally minimal so routing (Phase 1) has a real target.
 */
export default function TeacherDashboardPage() {
  return (
    <div className="mx-auto max-w-md">
      <h2 className="text-xl font-semibold">TeacherDashboard</h2>
      <p className="mt-2 text-sm text-muted-foreground">Menyusul di fase berikutnya.</p>
    </div>
  );
}
